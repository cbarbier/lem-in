import sys
import Tkinter as tk

def get_ants():
    line = sys.stdin.readline()  
    try:
        nb = int(line)
    except ValueError:
        print "number of ant is not an integer"
        sys.exit()

def get_nodes():
    nodes = {}
    for line in sys.stdin:
        if line == "##start"
            if line = sys.stdin
        print line

get_ants()
#app = tk.Tk()
#app.title("test")
#app.grid()
#app.mainloop()
